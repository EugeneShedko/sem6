public class Main {
	public static void main(String[] args)
	{
		int a = 5, b = 10;
        System.out.println(Math.pow(a, 2) - Math.pow(b,2));

        float c = 4f, d = 6f;
        System.out.println(Math.pow(c, 3) - Math.pow(d, 3));

        double e, f;
        e = 6;
        f = 8;
        System.out.println(Math.pow(e, 4) - Math.pow(f, 4));
		
		 char symbol1 = 'a', symbol2 = 'b';
		System.out.println(symbol1 - symbol2);
        System.out.println(symbol1 + symbol2);
	}
}